import { ActivityTypePipe } from './activity-type.pipe';

describe('ActivityTypePipe', () => {
    it('create an instance', () => {
        const pipe = new ActivityTypePipe();
        expect(pipe).toBeTruthy();
    });
});
