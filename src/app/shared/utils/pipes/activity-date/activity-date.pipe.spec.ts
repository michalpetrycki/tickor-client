import { ActivityDatePipe } from './activity-date.pipe';

describe('ActivityDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityDatePipe();
    expect(pipe).toBeTruthy();
  });
});
